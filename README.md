# MyLibrary1

This project demonstrates a simple dynamically linked library in C# which connects to a SQL Server database using ADO.NET (PostgreSQL).
You will not find any passwords here, as they are stored securely in environment variables.